# Reempaquetamos la imagen oficial de quay en Artifact Registry
FROM quay.io/keycloak/keycloak:latest

# (opcional) puedes subir a 24.x si usas esa versión
# ENTRYPOINT/CMD los trae la imagen; usaremos "start-dev" como args en Cloud Run
